# TOPIQ Migration Rules

This file is the permanent contract for the migration of `topiq_mockup_v25.html` into the React/Next.js dashboard.

> Tailwind token registry & reuse rules: see [TOPIQ_TAILWIND_TOKENS.md](./TOPIQ_TAILWIND_TOKENS.md) — consult it before any traditional-CSS → Tailwind conversion.

Teacher role icon: 🧑‍🏫 per owner, deviates from mockup 🍎

Dashboard shell is full-bleed by owner decision (100% width, no body padding, no border-radius/shadow) — deviates from mockup's framed 1180px card. Do not restore.

Dashboard main padding tightened to 8px top / 12px bottom desktop by owner decision.

## Rules

1. `topiq_mockup_v25.html` is the source of truth.
2. The migration must reproduce the mockup, not redesign it.
3. Existing landing/marketing pages must remain unchanged.
4. Existing routes must remain functional.
5. Dashboard code must be isolated from the existing marketing pages as much as reasonably possible.
6. Existing mockup CSS must be preserved.
7. Do not convert the CSS to Tailwind.
8. Do not rewrite CSS values merely for "cleanliness".
9. Do not change colors, spacing, dimensions, typography, borders, shadows, animations, breakpoints, or responsive behavior.
10. Do not remove functionality from the mockup.
11. Do not replace working mockup behavior with placeholder behavior.
12. Every vanilla JavaScript interaction must eventually have an equivalent React implementation.
13. Every screen in the mockup must eventually have an equivalent React screen.
14. React state should replace DOM manipulation where appropriate.
15. React event handlers should replace inline `onclick`, `onkeydown`, etc.
16. Mockup data should initially remain mock/local data unless I explicitly request API integration.
17. Do not connect APIs, databases, authentication, payments, or backend services during this visual migration.
18. Do not modify the mockup's authored content unless required by React syntax.
19. Do not create a second design based on your own interpretation.
20. When uncertain, inspect the original mockup before making a decision.
21. When a visual difference exists between the React version and the mockup, the mockup wins.
22. Never silently skip a screen or interaction.
23. Complete one migration phase at a time.
24. After each phase, report exactly what was changed.
25. Do not proceed to the next phase without my instruction.

## CURRENT MIGRATION STATUS

- Phase: 3 + Challenges + Subscription + Competitions + Workspace + Profile + Onboarding — First content screens + Practice + Sabi AI + Books/Library + Progress + Assignments + Live Class + Challenges + Subscription + Competitions + Workspace + Profile + Onboarding flow
- Status: Dashboard converted to page-per-navigation-app. Next.js App Router routes under /dashboard; Discover/Home, Subjects, Subject Hub, Practice (hub + session), Sabi AI (launcher, answer/chat, chat history, voice learning), Books/Library (list + book detail + document viewer), Progress, Student Assignments, Live Class, Challenges, Subscription/Upgrade, Competitions, Workspace/Plan-your-week, Profile (view + edit) + Badges & Certificates, and the pre-dashboard Onboarding flow all converted to React; remaining nav items route to placeholder pages
- Architecture: shared chrome + state lifted into `dashboard/layout.tsx` -> `DashboardProvider` (context) + `DashboardShell` (rail/topbar/tabbar/drawer/badge toast/certificate modal). Each navigation item is a real page route:
- Practice feature fully verified (headless Chrome automation): practice hub, by-subject/board drill-in, practice setup (duration/question-count steppers), single + mock sessions, question display, answer-selection feedback, countdown timer (hidden during English passage gate until read), question navigation (prev/next + q-palette jump), collapsible topic-grouped review cards, score/perf reports, mock multi-subject switcher with paper labels, exit-to-hub both modes.
- Sabi AI feature fully verified (headless Chrome automation, 30 checks across 3 suites): launcher (usage chip, voice-learn card, typed question -> own answer page), answer/chat screen (user bubble, grounded AI reply with § subject/topic chip + Listen button, usage chip + voice toggle hidden while chatting, new chat), chat history (list, live search filter, open item -> loaded into answer, delete), scan-question UI (launcher scan -> "Scanned a question from my textbook" + photo thumb; answer composer scan; AI reads the textbook question with ref chip), voice learning UI (Listening… -> transcript -> grounded answer + auto-speak), free-tier gating (3/day usage chip decrements, 4th question -> lock bubble with Unlock CTA, voice/scan same gate, Plus activation -> unlimited + voice mode unlock), voice-replies toggle (PLUS-locked -> upgrade modal for free users; auto-plays answer when on), subject auto-detection from syllabus topics + ambiguous-question subject suggestion chips (quickAsk).
- Books/Library feature fully verified (headless Chrome automation, 35 checks across 2 suites): Books list (search, Books/Textbooks type toggle, genre shelf chips filter, Spotlight row, per-genre shelf rows, subject grid in Textbooks view), book detail (hero header + genre, soft-copy file card with deterministic filename/pages/MB, Read button), document viewer modal (page indicator, page-sheet shell, prev/next with disabled states, slider jump, close), Practice box (maps genre -> subject practice handoff; unmapped genre shows "still being put together"), back-to-Books nav, subject cover handoff to subject hub, practice box handoff to practice setup via `?setup=subject`.
- Progress feature fully verified (headless Chrome automation, 18 checks): title/sub, stats row (12 day streak / 184 questions / 76% avg mastery), activity bar chart (7 cols, minutes [42,—,65,30,—,50,20], 2 missed bars, Mon–Sun labels), heatmap (per-subject rows × 7 cells matching mockup deterministic opacity formula, M/T/W/T/F/S/S day headers), mastery-by-subject rows + progress fills, plan-status-card -> /dashboard/upgrade, content-restored after browser back, back-row -> /dashboard. Visual DOM diff against mockup confirms identical plan-card text and identical heatmap cell opacity values.
- Student Assignments feature fully verified (headless Chrome automation, 12 checks): title/sub, 3 assignment cards in mock order (Photosynthesis / Simultaneous equations / Mole concept), due dates (This week/Today/Tomorrow), class metas (SS2 Biology / SS3 Mathematics / SS2 Chemistry), all Pending with Start CTA, Start -> `?setup=subject` practice setup handoff, submitted state (✓ Submitted + Review again →) persists across navigation via context (assignDone), Discover quick-card shows live pending count (3 pending default; 2 after marking one done).
- Live Class feature fully verified (headless Chrome automation, 29 checks): hub title Today's classes, subnav (Today/Upcoming/Attended/Missed), Today rows from Mon timetable (4), live row = Biology index 1 with pulsing dot + "Live — join →", join -> session view (hub hidden, live content shown), topbar (Photosynthesis · 🧬 Biology · SS2 Biology + live dot), Learn pane (article headings + takeaway), Practice pane CTA -> practice-session handoff, Interactive pane (poll question, 4 options, classmate avatars from ROSTERS, live count "N of 34 answered"), poll answer marks correct/wrong + renders pct fills (frozen once answered, not re-rolled), back -> hub, Upcoming/Attended/Missed panes from LIVE_CLASS_LOG with correct attendance badges. Visual DOM diff against mockup confirms identical topbar, learn headings, poll question + options, and avatars.
- Challenges feature fully verified (headless Chrome automation, 32 checks across 2 suites): title/eyebrow/back-row, active+completed sections hidden initially, 4 available challenge cards (7-Day Streak / 10 Questions This Week / Speed Round / Weak Spot Comeback) all Start-enabled, start -> Active card + "Active" disabled button + "0/10 questions" + 0% fill, give-up cancels back to Start, Discover quick-card sub ("4 to try" -> "N active"), sidebar rail/drawer `.rl-count` badge shows active count, practice answers drive `registerChallengeProgress` (count/accuracy/mastery), Speed Round completes at 5-in-a-row -> moves to Completed section with EARNED badge + `.done` card, Discover sub back to "to try" state, state persists across in-app navigation via `DashboardContext`.
- Subscription/Upgrade feature fully verified (headless Chrome automation, 30 checks): back-row -> Progress, eyebrow "Your plan", title "Subscription", page-sub, plan-status-card (Free plan -> "3/3 Sabi AI questions left today · Upgrade for unlimited"), "Free vs. AbSTopiq Plus" card (4 school-feature-rows: Practice flashcards lessons / Sabi AI chat & scan / Audio narration & voice replies / Timed mock exams), "Choose a plan" eyebrow, plan-toggle-row with Monthly (₦1,500 / per month) + Exam-Ready Pass (₦2,000 / through results day, WAEC SEASON badge, `.best`), "Start 7-day free trial →" CTA, referral card (abstopiq.app/join/CORONA2026 + Copy -> "Copied ✓" flash), select plan/activate Plus -> status card becomes "✨ AbSTopiq Plus / Unlimited Sabi AI, audio & mock exams", eyebrow -> "You're all set", plan cards + CTA hidden, state persists back on Progress plan-status-card, responsive rail layout, dark-mode rendering. Plan status card state shared via `DashboardContext` (`isPlusUser`/`freeAiUsesLeft`/`activatePlus`) and reused by the Progress screen's `progressPlanCard`.
- Competitions feature fully verified (headless Chrome automation, 23 checks): title "🏆 Competitions", eyebrow "Beyond the syllabus", back-row -> Discover, page-sub, 5 competition cards in mockup order (IMO / IChO / IBO / PAMO / Cowbellpedia), each with `.comp-icon`/`.comp-name`/`.comp-level` (Global/Global/Global/Regional/National · Nigeria)/`.comp-desc`/`.comp-meta-row` tags (`{subject icon} {subject}` + `🗓 {months}`)/`Start a prep track →` CTA, CTA -> `startSession` single-subject practice-session handoff (board = subject's first board, 20 min, 10 questions), nav to/from Discover/Progress/Challenges intact, 5 cards after session, responsive rail layout, dark-mode rendering. COMPETITIONS data reused from `data/challenges.ts` (verbatim from mockup 5677–5683).
- Workspace feature fully verified (headless Chrome automation, 44 checks across 2 suites): nav `Plan` (`/dashboard/plan`), eyebrow "Your mini workspace", title "Plan your week", page-sub, week-glance (Mon–Sun labels, today `•`, active-day click -> Timetable pane for that day), 4 pane tabs (Calendar/Timetable/To-do/Exams), Calendar pane (month label, weekday row, 36-cell grid, today marked, task/exam `cal-dot`s, day click -> `.selected` + "On this day" agenda rows, ‹/› month shift), Timetable pane (per-day `topic-row`s with colored subject icon + time `qtag`, "+ Add a class"), To-do pane (`recommend-card` "Due today · N tasks left", Today/Tomorrow/Later/No date groups, `plan-check` toggle done -> strike/0.5 opacity, recurring task -> reschedule to next due + toast, colored `plan-why` chips), Exams pane (3 plan-items sorted by days-left, `14d left` coral / `21d left` ember / `33d left` thread countdown chips, "+ Add an exam"), quick-add modal (task/class/exam modes, subject/board chips, priority/repeat/date, validation, adds to state + lists live), timetable click -> subject hub, exam item -> `startSession` practice handoff, state persists across in-app navigation via `DashboardContext` (timetable/tasks/exams), responsive rail layout, dark-mode rendering. TIMETABLE/TASKS/EXAMS data reused from `data/workspace.ts` (verbatim from mockup 4359–4380).
- Profile feature fully verified (headless Chrome automation, 59 checks across 3 suites): nav via rail/drawer account (`/dashboard/profile`), back-row -> Progress, eyebrow "Your account", title "Profile", page-sub, avatar (placeholder -> uploaded image), full name + `@username`, 9-row personal-info card (First name / Last name / Age / Date of birth / Phone number / Grade level / Gender / Class of study / School "Not linked — individual account"), "✎ Edit profile" -> edit view (avatar picker + camera badge upload, username `@` field, first/last name, age, dob, phone, Curriculum Nigerian (JSS/SS) vs International (Grade 9–12) toggle, grade grid, gender, class-of-study track shown only for SS1–3 / Grade 11–12), username sanitization (lowercase + `[^a-z0-9_]` strip), Save -> updates view + rail/drawer avatar + name, Cancel discards, "🏆 Badges & Certificates" -> `/dashboard/badges` (certificate placeholder + 2 reverse-ordered topic badges with icon/topic/subject·date), "⚙ Settings" -> `/dashboard/settings`, edits persist across in-app navigation via `DashboardContext` (`profile`), responsive rail layout, dark-mode rendering. Profile data from `data/student.ts` (Chidinma Okafor / chidinma_o / SS2 / Science / Female / phone "0803 XXX XX21"; earnedBadges verbatim from mockup 6938–6941).
- Onboarding flow fully verified (headless Chrome automation, 78 checks): pre-dashboard overlay mounted above the shell at layout level, shown on first load and hidden once completed (stored completion in localStorage to avoid SSR/hydration overlay mismatch — default closed, opened by effect when the flag is absent). Auth step (logo, "Learn it. Practice it. Sabi it.", sub, Continue with Google/Apple/Facebook -> role, legal text). Role step ("Who's this for?", sub, exactly 3 selectable cards Student/Teacher/School-Institution with `.selected`). Student path -> details (avatar picker upload preview, username `@chidinma_o`/first `Chidinma`/last `Okafor`/age `16`/dob/phone `080X XXX XXXX` placeholders, "Next →") -> details2 ("Almost done", Nigerian JSS1–SS3 vs International Grade 9–12 curriculum toggle, gender Female/Male, conditional Class of study Science/Arts/Commercial via OB_TRACK_GRADES, school code `e.g. CORONA2026`) -> Finish -> `/dashboard` Discover, student mode. Teacher path -> details (first `Funmilayo`/last `Adeyemi`/phone, subject grid from SUBJECTS with multi-select `.context-chip.active`, school code with exact help text) -> Finish (`teacherdash` destination; "Covered by Corona Secondary School" on valid code, teacher mode). School path -> details (Institution name `Corona Secondary School`, Contact person `Full name`, phone, students `e.g. 250`, headline "Tell us about your school" + sub "One profile covers your whole institution.") -> Finish -> `/dashboard/schooladmin`, school mode. Back-nav auth<-role<-details<-details2. Progress dots obDot1-4 (hidden on auth, done per step ordinal). Onboarding hides after completion; appMode selects the correct nav set (procedure persisted in DashboardContext `appMode` + `NAV[appMode]`); school code associated to `studentSchool`/`teacherSchool` (school shown on Profile School row via renderProfile-equivalent). Reused data: OB_GRADES/OB_TRACK_GRADES from `data/student.ts`, SUBJECTS, SCHOOLS from `data/teacher.ts`.
- Account Switcher fully verified (headless Chrome automation, 26 checks across tests 1–10): the `brand-switch-btn` down-arrow (desktop rail, mobile topbar, mobile drawer) opens a React-state `#accountSwitchModal` translating `openAccountSwitch()`/`closeAccountSwitch()`/`switchAccount`/`LINKED_ACCOUNTS` (mockup 5712–5737). Data-driven rows (🎓 Chidinma Okafor/Student, 🧑‍🏫 Mrs. F. Adeyemi/Teacher, 🏫 Corona Secondary School/School Admin); the active row follows shared `appMode` with " · current" + checkmark; row click -> close modal, `if(mode===appMode) return`, else `setAppMode` + `goTab` to teacherdash/schooladmin/discover; ✕ closes; "+ Add another account" closes then `logout()` -> onboarding; `openAccountSwitch` calls `closeDrawer()` first so the mobile drawer closes before the modal (never two overlays); no second mode state — reuses `DashboardContext.appMode`. Reuses `.modal-overlay.show`/`.modal-sheet`/`.modal-close`/`.roster-row`/`.roster-avatar`/`.roster-name`/`.add-entry-btn` (no Tailwind).
- Completed: app shell, topbar, desktop rail, mobile tabbar + drawer, main scaffold, dark-mode/theme foundation
- Completed screens: Discover/Home, Subjects, Subject Hub, Practice (Practice hub + full practice/mock session), Sabi AI (launcher, answer/chat page, full chat history, voice learning), Books/Library (Books list, Book detail, Document viewer), Progress, Student Assignments, Live Class (hub Today/Upcoming/Attended/Missed + live session Learn/Practice/Interactive), Challenges (push-yourself header, In progress / Start a challenge / Completed sections, mastery/accuracy/count/streak progress, EARNED badge, give-up cancel, Discover quick-card + sidebar counts), Subscription/Upgrade (current-plan status card, Free vs. AbSTopiq Plus comparison, Monthly ₦1,500 + Exam-Ready Pass ₦2,000 plan cards, Start 7-day free trial CTA, school referral link copy, mock Plus activation), Competitions (5 competition cards in mockup order, per-card icon/name/level/desc/meta tags, Start a prep track -> practice session handoff, back -> Discover), Workspace/Plan-your-week (week-glance, Calendar/Timetable/To-do/Exams panes, quick-add modal, timetable -> subject hub, exam -> practice handoff, live state via context), Profile (view + edit + avatar upload + username sanitize + curriculum/grade/gender/track pickers, rail/drawer account reflects profile) and Badges & Certificates (certificate placeholder + topic badges) (user-initiated pane switching, topic filtering, flashcards, hub chat, search, subject navigation, article/tutorial modal)
- Completed interactions summary: tab/screen switching & nav active state, drawer open/close, theme state -> html[data-theme]; Discover search dropdown, subject chips + topic list, quick cards & promo navigation, adaptive "continue" hero; Subjects grid navigation; Hub back nav, Overview/Learn/Flash panes, topic search + list, tutorial render + article modal open, flashcard prev/next/flip/rate/review loop, hub chat composer, article modal (audio player, video mock, quick-check, term grid, takeaway, mark-as-done updating mastery + badge toast + certificate modal); Practice hub (Nigerian/International/By-Subject panes, board drill-in, mock picker with compulsory English + subject chips + year + full/quick type + running total/breakdown, practice setup with year/duration/question-count steppers); Practice session (practice/mock banner, subject switch for mock, passage gate, question track/score/clock, answer-selection feedback, q-palette jump, prev/next, mock multi-subject progress save/load/next-incomplete, mock & single score/perf report grouped by topic/subject, strengths, weak points with study navigation, collapsible review cards, practice-again, back-to-practice)
- Known differences:
  - brand mark uses public/logo.png (replaces mockup's inline base64 PNG)
  - marketing chrome (NavBar/Footer/CookieBanner) hidden on /dashboard via client AppShell wrapper instead of a route-group layout (keeps marketing files untouched)
  - dark-mode visual toggle deferred to Settings screen; the state + effect are in place
  - teacher/school nav configs present in navConfig but mode switching not yet wired (student active)
  - audio/video players and built-in speak-play render/playstate implemented for testing; the mockup's free-tier "upgrade" gate (isPlusUser/studentSchool -> upgrade modal) is not wired in this batch (upgrade/school are out of batch) — playback works so interactions are testable
  - hub/ASK chat uses the canned grounded reply (no backend); free-use gating (3/day) IS enforced in React (Sabi AI: usage chip, lock bubbles, Plus activation) but resets on page load (no persistence)
  - participation tracking (participated list) is wired in React (`openSubject` adds the subject; Practice "By Subject" pane shows the ✅ Participated tag) but is not persisted to any backend
  - Live Class `lsAddClassBtn` (teacher-only quick-add) is omitted on the student-facing converter; teacher/live-management side (startLiveForClass, endLiveSession, nextLivePoll teacher row) is out of batch scope
  - assignment pending count on Discover/rail is driven live from `assignDone` context state (matches mockup's `renderStudentAssignments` updating `qcAssignSub`); Discover quick-card challenge sub + sidebar `.rl-count` challenge badge are driven live from `activeChallenges` context state
  - subscription state (`isPlusUser`/`freeAiUsesLeft`/`activatePlus`) is lifted into `DashboardContext` and shared by the Subscription screen + Progress `progressPlanCard`; the mockup's `studentSchool` "covered by school" variant is not wired (school onboarding out of batch scope), and Sabi AI's upgrade modal keeps its own local plus state
  - Workspace `.rc-round`/week-glance header meta (`x sessions this week`, `x tasks due`) renders as the static mockup string; the `studentSchool` timetable variant (adds "🏫 School" chip + "+ Add a personal study block" button text) is not wired (school role out of batch scope), so the toolbar uses the base "+ Add a class"; exam quick-add `days` uses a normalized map (1 week→7, 2 weeks→14, 3 weeks→21, 1 month→30, 2 months→60) instead of calendar math
  - Subscription/Competitions/Workspace confirmed UNTOUCHED by the other phases: only their own phase touched their files; shared `DashboardContext.tsx` gains new state but existing subscription/competition behavior is preserved (verified green in every regression run)
  - Profile: the badge/certificate rows are read from the static `earnedBadges`/`earnedCertificates` arrays (identical to mockup init data); the mockup's `studentSchool` "covered by school" variant stays hardcoded as "Not linked — individual account" (school onboarding out of batch scope); the `Settings` button navigates to the placeholder `/dashboard/settings` route rather than a built Settings screen (Settings is intentionally out of this Profile phase)

### Data migration (sub-phase complete)
- Migrated ALL authored mock data structures (values, order, IDs, labels, icons, relationships preserved 1:1) from the mockup `<script>` into `src/app/dashboard/data/`:
  - `subjects.ts`: SUBJECTS, SUBJECT_DIAGRAMS
  - `books.ts`: BOOK_GENRES, BOOK_CATALOG, GENRE_SUBJECT_MAP
  - `chat.ts`: chatHistoryData, VOICE_SAMPLE_QUESTIONS
  - `workspace.ts`: TIMETABLE, TASKS, EXAMS, STUDY_ACTIVITY_MINUTES
  - `exams.ts`: EXAM_BOARDS, NIGERIA_BOARDS, INTL_BOARDS, MOCK_TYPES, MOCK_SUBJECT_LIMIT
  - `student.ts`: studentProfile, earnedBadges, earnedCertificates, LINKED_ACCOUNTS, OB_GRADES, OB_TRACK_GRADES, FREE_AI_DAILY
  - `challenges.ts`: CHALLENGE_TEMPLATES, COMPETITIONS
  - `teacher.ts`: SCHOOLS, SCHOOL_TEACHERS, SCHOOL_PENDING_STUDENTS, CLASSES, ROSTERS, ASSIGNMENTS, TEACHER_TODAY_SCHEDULE, LIVE_CLASS_LOG, LP_TYPES, LP_DURATIONS, LP_HISTORY, FREE_TEACHER_ASSIGNMENTS
  - `index.ts`: re-exports all 37 names
- Not migrated (runtime/derived, not authored data): SEARCH_INDEX (derived via buildSearchIndex), runtime state vars (currentSubject, sessionIndex, mockMode, activeChallenges, etc.), functions.
- Data sourced from the mockup's init-time values (studentProfile/earnedBadges use their seeded 6937–6941 values, not the empty placeholders in the object-template).
- Verified: `npm run typecheck` and `npm run build` pass.